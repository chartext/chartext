import { describe, it } from 'vitest';

/*
import { BoxPlotData, PointData } from './ChartTypes';
const computeQuantiles = require('compute-quantiles');

test('Box plot data test', () => {
  const one = new Date('2022-07-01');
  const many = new Array(1000);

  for (let i = 0; i < many.length; i++) {
    many[i] = Math.round(Math.random() * 1000);
  }

  const quantiles = computeQuantiles(many, 4);

  const boxPlotData: BoxPlotData<Date> = {
    one: one,
    q0: quantiles[0],
    q1: quantiles[1],
    q2: quantiles[2],
    q3: quantiles[3],
    q4: quantiles[4]
  };

  console.log(boxPlotData);
});

test('Point data test', () => {
  const x = new Date('2022-08-01');
  const y = Math.round(Math.random() * 1000);

  const pointData: PointData<Date, number> = { x: x, y: y };

  console.log(pointData);
});
*/
describe('Plot test', () => {
  it.skip('foo', () => {
    //
  });
});
